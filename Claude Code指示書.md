# Claude Code 指示書

このファイルは、作業内容に応じて該当する章だけを Claude Code に指示する。
プロジェクト全体の前提は `CLAUDE.md` に記載済み（Claude Code が自動で読む）。

---

## 【最優先】タスク0：まず動かす

**これが終わるまで、以下のタスク1〜4には着手しない。**

```
現状のコードは実環境で未検証です。まず以下を実施してください。

1. requirements.txt の依存を実際にインストールし、バージョン競合がないか確認
2. 30秒程度の短い音源で bandcopy.py をエンドツーエンド実行
   （テスト音源がなければ、生成した簡単なWAVでも可）
3. 各段階（テンポ検出／分離／採譜／簡略化／MusicXML出力）が
   実際に通るか確認し、落ちた箇所を修正
4. Basic Pitch の戻り値の型・API が現行バージョンと合っているか検証
   （predict() の引数名やmodel pathの指定方法が変わっている可能性あり）
5. music21 の MusicXML 出力が MuseScore で開ける形式か確認

修正した箇所は、何がどう違ったかを説明してください。
```

---

## タスク1：タブ譜出力

```
bandcopy.py に --tab オプションを追加し、ギター/ベースパートを
タブ譜（GuitarPro形式）で出力できるようにしてください。

要件:
- ライブラリは PyGuitarPro を使用
- MIDI のピッチから弦・フレット位置を決定する必要がある。
  同じ音高が複数の弦で出せるため、運指最適化を実装すること
  - 手の移動距離が最小になる押さえ方を選ぶ（動的計画法）
  - --max-fret オプションで使用フレットの上限を指定できるようにする
    （例: --max-fret 5 なら1〜5フレット内に収める＝難易度低下に直結）
  - 開放弦を優先的に使う（押さえる本数が減るため）
- ベースは4弦（E1 A1 D2 G2）、ギターは6弦（E2 A2 D3 G3 B3 E4）
- チューニングを --tuning オプションで変更可能に（drop D 等）
- 出力先は score/ フォルダ、拡張子は .gp5

この運指最適化が難易度調整と直結する部分なので、
--max-fret を変えたときに実際に運指がどう変わるかを
テストで確認できるようにしてください。
```

---

## タスク2：スコア譜統合

```
bandcopy.py に --score オプションを追加してください。

要件:
- 各パートのMIDIを music21 で1つの Score にまとめる
- パートごとに Part オブジェクトを作り、楽器名（Bass, Guitar, Vocal）を設定
- 適切な音部記号を割り当てる
  （ベース=ヘ音記号、ギター=ト音記号（オクターブ表記）、ボーカル=ト音記号）
- 小節線・拍子が正しく入るよう、検出したテンポと拍子を反映
- MusicXML と、可能ならPDFの両方で出力
- PDF化は MuseScore の CLI（mscore -o out.pdf in.musicxml）を利用。
  MuseScore が未インストールの場合はスキップしてその旨を表示
```

---

## タスク3：ドラム譜対応

```
現在ドラムは音源分離のみで採譜していません（Basic Pitch は音程楽器用のため）。
ドラム譜生成を追加してください。

アプローチ:
- Basic Pitch は使わない。オンセット検出＋音色分類で実装する
- librosa.onset.onset_detect で打点を検出
- 各打点の周波数特性から、キック／スネア／ハイハット／タム／シンバルを分類
  （まずは周波数帯域ベースの単純なルールで可。低域=キック、
    中域+ノイズ=スネア、高域=ハイハット）
- 分類結果を General MIDI のドラムマップ（ch.10）に割り当て
- simplify.py の簡略化を適用する
  - レベルが低いほどフィルインを基本ビートに置換する処理を追加
- music21 でドラム譜として出力

精度は完璧でなくてよい。「フィルの位置と基本ビートが分かる」レベルを目標に。
```

---

## タスク4：Webアプリ化

```
※ タスク0〜2が完了し、実際にバンドで使って有用性を確認してから着手すること。

構成:
- バックエンド: FastAPI
- フロントエンド: React（シンプルなSPAで可）
- 公開方法: 自宅PC + Cloudflare Tunnel を想定（クラウドGPUは使わない）

要件:
- ユーザーが音源ファイルをアップロードする方式にすること
  （サーバー側で動画サイトから取得する機能は実装しない）
- 処理は非同期。ジョブキューで管理し、進捗をフロントに表示
  （Demucsは5〜15分かかるため、同期処理にはしない）
- 難易度レベルをUIのスライダーで選択できるように
- 完了後、パート別音源・MIDI・楽譜をブラウザからダウンロード可能に
- 同時実行数を1に制限（メモリ不足を避けるため）
- アップロードされたファイルは処理完了後N時間で自動削除
```

---

## 進め方の指示（共通）

```
- タスクは1つずつ進め、都度動作確認してからコミットしてください
- 大きな変更の前に、何をどう変えるかの方針を先に提示してください
- エラーメッセージは非エンジニアが読む前提で、平易な日本語にしてください
- 機能を追加したら 手順書.md にも反映してください
- 動作確認は短い音源（30秒程度）で行い、フル尺での検証は最後にまとめて
```
