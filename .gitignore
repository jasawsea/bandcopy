venv/
output/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# 大きなメディア（再取得/再生成できるため追跡しない）
#   audio/*.mp3 は getaudio.py で再取得、test30.wav は合成音で再生成可
audio/
*.wav

# git worktree（実装用の隔離ワークスペース）
.worktrees/
.superpowers/

# worktree用のsymlink（実体は本チェックアウト側）
/venv
/output
/audio

# Gradio共有トンネルが実行時に取得するルート証明書
.gradio/

# Wordが開いている間に作る一時ロックファイル
~$*

# 手順書のWord版。原本は 手順書.md（そちらがgit管理下の唯一の正）。
# docx は配布用に md から作り直せるので追跡しない（二重管理で片方だけ古くなるのを防ぐ）
手順書.docx
*.docx

# ダウンロードしたモデルの重み（335MB・再取得できるので追跡しない）
models/
