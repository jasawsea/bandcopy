# バンドコピー支援ツール 手順書（改訂版）

曲の音源から、**パートごとに分離した練習音源**と**演奏しやすい難易度の楽譜**を自動で作るツールです。

ツールは2本あります。

| ファイル | 役割 |
|---|---|
| `getaudio.py` | 動画URLから音声を取り出してMP3にする |
| `bandcopy.py` | MP3を分離・採譜して、簡略化した楽譜にする |

`getaudio.py` は補助ツールなので、手持ちのMP3から始める場合は不要です。

---

# 第1部 準備編（最初の1回だけ）

所要時間の目安：30分〜1時間

## 1-1. Pythonのバージョンを確認する

Macは「ターミナル」、Windowsは「コマンドプロンプト」を開いて、以下を貼り付けてEnter。

```
python3 --version
```

**`Python 3.10` 〜 `3.12` のどれかが出ればOKです。**

| 表示 | 対処 |
|---|---|
| `3.10` 〜 `3.12` | そのまま次へ |
| `3.13` 以上 | 3.12を別途インストール（採譜ライブラリが未対応） |
| エラー／何も出ない | [python.org](https://www.python.org/downloads/) から3.12をインストール |

> ここを飛ばすと、あとで必ずエラーになります。先に確認してください。

---

## 1-2. ffmpeg を入れる

音声変換に必要な、裏方のソフトです。

**Mac:**
```
brew install ffmpeg
```
（`brew` が無いと言われたら、先に [brew.sh](https://brew.sh/) の手順でHomebrewを入れる）

**Windows:**
```
winget install ffmpeg
```

**確認:**
```
ffmpeg -version
```
ずらっと文字が出れば成功です。

---

## 1-3. 作業フォルダを用意する

配布ファイル一式（`bandcopy.py` `simplify.py` `getaudio.py` `requirements.txt`）を、わかりやすい場所に置きます。

例：`書類 > bandcopy` フォルダを作って、そこに全部入れる。

ターミナルでそのフォルダに移動します。

```
cd ~/Documents/bandcopy
```

> **簡単なやり方**：ターミナルに `cd ` とだけ打って半角スペースを入れ、フォルダをターミナルの窓にドラッグ&ドロップするとパスが自動入力されます。

---

## 1-4. 専用の作業部屋を作る（仮想環境）

```
python3 -m venv venv
```

数秒で終わり、`venv` というフォルダができます。

> これは「このツール専用の隔離された部屋」です。パソコン全体の環境を汚さないので、不要になったら `venv` フォルダを削除するだけで完全に元通りになります。

続いて、その部屋に**入ります**。

**Mac:**
```
source venv/bin/activate
```

**Windows:**
```
venv\Scripts\activate
```

**成功すると、行の先頭に `(venv)` と表示されます。**

```
(venv) ~/Documents/bandcopy $
```

⚠️ **この `(venv)` 表示が最重要です。** ターミナルを閉じると消えるので、**次回以降も作業のたびにこのコマンドを実行**してください。エラーの原因の大半はこれの入れ忘れです。

---

## 1-5. 必要なソフトを一括インストール

```
pip install -r requirements.txt
```

**5〜15分かかります。** 大量の文字が流れますが正常です。

**確認:**
```
python3 -c "import demucs, basic_pitch, music21, yt_dlp; print('準備OK')"
```

`準備OK` と出れば準備完了です。

---

# 第2部 使い方編

## まずは「お試し1曲」から

いきなり1曲まるごと処理すると、初回は20分近くかかります。

**最初は30秒だけ切り出して試すのを強くおすすめします。** 精度が合わなければ設定を変えるだけで済み、待ち時間が数分で済みます。

---

## 2-1. 音源を用意する

### パターンA：手持ちのMP3を使う（推奨）

作業フォルダに音源ファイルを置くだけです。

**ファイル名は半角英数にしてください。** 日本語やスペースが入るとエラーの原因になります。

例：`song01.mp3`

### パターンB：動画URLから取り出す

```
python3 getaudio.py "https://..." --quality 320 --ascii-name
```

`audio` フォルダにMP3ができます。

| オプション | 意味 |
|---|---|
| `--quality 320` | 高音質。**分離・採譜に使うなら必須級**（音質が低いと分離が汚くなる） |
| `--ascii-name` | ファイル名を英数字にする。後続処理でコケにくくなる |

**サビだけ切り出す（お試しに最適）:**
```
python3 getaudio.py "https://..." --quality 320 --ascii-name --start 1:20 --end 1:50
```

**中身を確認だけしたいとき:**
```
python3 getaudio.py "https://..." --info
```

---

## 2-2. 分離・採譜・楽譜化を実行する

`(venv)` が表示されている状態で:

```
python3 bandcopy.py song01.mp3
```

これだけです。

### 画面に出るもの

```
[0/4] テンポを解析中...
      推定テンポ: 128.0 BPM
[1/4] パート分離中...
      ✓ ドラム
      ✓ ベース
      ✓ ギター・キーボード等
      ✓ ボーカル
[2/4] 採譜中（3パート）...
[3/4] 難易度レベル3に簡略化中...
      → ベース
  [簡略化] 標準（16分グリッド・和音3音まで）
           音符数 1240 → 780（460個削減 / 37%減）
[4/4] 楽譜（MusicXML）に変換中...
完了しました。
```

**初回は10〜20分**（AIモデルを自動ダウンロードするため）。2回目以降は3〜5分です。

> 途中で止まったように見えても、初回は待ってください。数百MBのダウンロード中です。

---

## 2-3. できたものを確認する

`output` フォルダの中身：

```
output/song01/
  ├── stems/                          ← 分離した音源
  │     ドラム.wav
  │     ベース.wav
  │     ギター・キーボード等.wav
  │     ボーカル.wav
  │
  ├── midi/                           ← 音符データ
  │     ベース_原曲どおり.mid
  │     ベース_Lv3.mid                 ← 簡略化したもの
  │
  └── score/                          ← 楽譜
        ベース_Lv3.musicxml
```

**最初に `stems/` の音源を聴いてください。**

これだけでも「ベースだけ聴く」「ドラムだけ聴く」ができるので、耳コピが一気に楽になります。楽譜がイマイチでも、ここは確実に価値があります。

---

## 2-4. 楽譜を開く

`.musicxml` を開くには **MuseScore**（無料）を使います。

1. [musescore.org](https://musescore.org/) から MuseScore Studio をダウンロード
2. インストール
3. `.musicxml` をダブルクリック

楽譜が表示され、**再生・編集・PDF書き出し**ができます。メンバーへの配布はここからPDFにするのが早いです。

---

# 第3部 調整編

## 難易度を変える

出てきた楽譜が合わなければ、数字を変えて再実行します。

```
python3 bandcopy.py song01.mp3 --level 2
```

| 指定 | 内容 | 使いどころ |
|---|---|---|
| `--level 5` | 原曲どおり（無加工） | まず原曲の音符を全部見たい |
| `--level 4` | やや簡略 | 少し間引きたい |
| `--level 3` | **標準（既定）** | まずここから |
| `--level 2` | かなり簡単 | 初心者メンバーがいる |
| `--level 1` | 単音のみ | とにかく形にする |

**進め方のコツ**：まず `--level 3` で作ってメンバーに見せる。「まだキツい」と言われたら `--level 2` で作り直す。両方作って並べて比べるのが確実です。

### 簡略化で何をしているか

1. 音符を8分/16分のグリッドに揃える（リズムのゆらぎを整理）
2. 短すぎる装飾音を削除
3. 和音の音数を減らす（ベースは低い音、ボーカルは高い音を優先して残す）
4. 同じ音の連打をまとめる

---

## パートを絞る（時間短縮）

```
python3 bandcopy.py song01.mp3 --parts bass
```

指定できるのは `bass`（ベース）/ `other`（ギター等）/ `vocals`（ボーカル）。

**ベースだけ、ギターだけを試したいときは必ず指定してください。** 処理時間が1/3になります。

---

## テンポを手動指定する

```
python3 bandcopy.py song01.mp3 --tempo 132
```

**楽譜のリズムがぐちゃぐちゃに見えたら、まずここを疑ってください。** テンポ検出が半分や2倍にズレていることがよくあります。

---

# 第4部 困ったとき

| 症状 | 原因と対処 |
|---|---|
| `command not found: python3` | Pythonが未インストール |
| `ModuleNotFoundError` | **`(venv)` が出ていない**。`source venv/bin/activate` をやり直す |
| `basic_pitch` のインストール失敗 | Python 3.13を使っている。3.12を入れる |
| `ffmpeg not found` | ffmpeg未インストール。1-2を実行 |
| ファイルが見つからない | MP3が作業フォルダにあるか確認。ファイル名を半角英数に |
| 分離した音がにごる | 元音源の音質が低い。320kbps以上を使う |
| 楽譜のリズムが変 | テンポ検出ミス。`--tempo` で手動指定 |
| 音符が多すぎて読めない | `--level` の数字を下げる |
| 処理が終わらない | 初回はモデルDL中。20分は待つ |
| 動画のダウンロードが失敗する | yt-dlpを更新：`pip install -U yt-dlp` |

---

# 第5部 大事な前提

## 精度は7〜8割

**このツールは完璧な楽譜を作るものではありません。**

特に歪んだギターや複雑な和音は苦手です。正しい使い方はこうです。

> **AIに8割やらせて、残り2割を耳と手で直す**

ゼロから耳コピするより作業時間は大幅に減ります。MuseScoreで開いて、明らかにおかしい音符を直しながら仕上げていく——これが現実的な進め方です。

## 権利のこと

**バンド内の練習用に使う分には問題ありません**（私的複製の範囲）。

ただし以下は権利者の許諾が必要です。

- 作った楽譜を配布・販売する
- 分離した音源を公開する
- サービスとして他人に提供する

また、動画サイトからのダウンロードは、**無許諾アップロードと知りながら行うと私的複製の例外から外れます**（日本の著作権法）。手持ちのCDや購入した音源を使うのが最も安全です。

**将来アプリ化してメンバーに配る場合**は、「各自が自分の音源をアップロードする」設計にしておくと、この論点を避けられます。

---

# 第6部 まず何をするか

1. **第1部の準備をすべて済ませる**（30分〜1時間）
2. **好きな曲のサビ30秒だけ切り出す**
   ```
   python3 getaudio.py "URL" --quality 320 --ascii-name --start 1:20 --end 1:50
   ```
3. **ベースだけ、レベル3で処理する**
   ```
   python3 bandcopy.py audio/xxxxx.mp3 --parts bass --level 3
   ```
4. **`stems/ベース.wav` を聴く** → 分離の精度を確認
5. **`score/` の楽譜をMuseScoreで開く** → 採譜の精度を確認

ここまでで**実用に耐えるかどうかの判断がつきます**。

耐えるなら、フル尺・全パートに広げる。耐えないなら、その時点で何が足りないかを持ち帰って改良する——という順序が一番ムダがありません。

---

# 今後の拡張候補

- **タブ譜出力** — ギター/ベースのフレット位置を最適化してGuitarPro形式で出力
- **スコア譜統合** — 全パートを1つの譜面にまとめる
- **ドラム譜対応** — 現状は音源分離のみ（採譜には別方式が必要）
- **PDF一括出力** — MuseScoreのコマンドライン機能で自動化
- **アプリ化** — まずは出力フォルダをGoogle Drive共有する運用から
